# Optional: point the mobile app at a local Rails API (same machine: use your LAN IP, not localhost).
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.10:3000/api/v1

# Production default is baked into src/config.ts if unset.
