<% if @reason == :admin_provisioned %>
Your TechFlash company account is ready

An administrator created an account for <%= @user.email %>.

Choose your own password using this link (expires in 72 hours):
<%= @reset_url %>
<% else %>
Reset your TechFlash password

We received a request to reset the password for <%= @user.email %>.

If you did not ask for this, ignore this email.

Open this link to set a new password (expires in 72 hours):
<%= @reset_url %>
<% end %>
